1_섹션 - Transfrom
개념 - 하드웨어 가속(GPU)를 사용하기 때문에 position : absolute 를 사용하여 움직이는 것보다 훨씬 더 부드럽고 빠르게 작동함
transition : css로 애니메이션을 줄 수 있는 속성, 값이 수치로 표현되는 속성의 변화가 있을 때 그 변화의 중간 과정을 애니메이션으로 만들어줌 ex) size가 2배가 되면 천천히 2배가 되는
애니메이션을 실행해줌, 색과 같은 값도 수치로 표현되므로 애니메이션 생성 가능 반면에 width : auto;처럼 숫자로 표현되지 않는 속성은 애니메이션이 만들어지지 않음
활용 - 기본으로 100px을 주고 hover시 200px을 주면 사이즈가 커지는 애니메이션을 자동으로 만들어줌

2_섹션 - Animation
keyfrome 
1. 이동 중 방향을 바꾸는 등 변화가 있는 지점을 부르는 말, 트랜지션을 중간에 변화를 주는 기능이 없음
2. 함수처럼 이름이 있으며 @keyframes 이름 의 형식으로 선언 가능, 시작이 0% 끝이 100%인 타임라인이 있으며 원하는 지점을 %로 지정할 수 있음
3. animation : 원하는 오브젝트에 name time 형식으로 으로 동작 가능